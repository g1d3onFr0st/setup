cl(){
    cd "$1" && ls
}
cla(){
    cd "$1" && ls -a
}
cdc(){
    cd "$1" && clear
}
cll(){
    cd "$1" && ls -l
}
clla(){
    cd "$1" && ls -la
}
mkcd(){
    mkdir -p "$1" && cd "$1"
}
















